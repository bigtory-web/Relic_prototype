# Relic_prototype

Relic prototype game project.

## Project Location

- OneDrive: `OneDrive/[project folder]/game project/relic_prototype`
- GitHub: https://github.com/bigtory-web/Relic_prototype

## Current Status

- Public GitHub repository is connected.
- Local project folder is stored inside OneDrive for easier access from other PCs.
- The first playable relic management prototype is implemented as a single HTML file.
- The prototype includes equipped synergy summary, synergy stage preview, stabilized slot layout, and a clearable synergy selection modal.
- Korean text and relic data labels in `index.html` were restored and normalized to UTF-8.
- The relic catalog now contains 22 relics across `N 4 / R 4 / SR 6 / SSR 8`.
- Unowned relics can be inspected, shard progress is shown in inventory, and upgrade preview values are shown in detail.

## How To Run

- Open `index.html` in a browser.
- Or run a simple local server such as `python -m http.server 4173` and open `http://127.0.0.1:4173/`.
- The prototype is designed for a fixed `1280 x 720` desktop browser viewport.

## Next Steps

- Review the relic synergy balance values and stage names.
- Add final portfolio screenshots after the planning document structure is fixed.
- Decide whether the upgrade formula should remain fixed-value or become grade-based in the final document.
